# Docker Hub Configuration
# Your Docker Hub username for pushing/pulling images
DOCKER_USERNAME=your_dockerhub_username

# MongoDB Configuration
# MongoDB root username for authentication
MONGO_USERNAME=admin
# MongoDB root password (change this to a secure password)
MONGO_PASSWORD=secure_password_here
# MongoDB database name for the application
MONGO_DATABASE=meanapp

# Backend Configuration
# Node.js environment mode (production, development, or test)
NODE_ENV=production
# Port number for the backend API server
PORT=8080

# Frontend Configuration
# Backend API URL for frontend to connect to
API_URL=http://localhost/api
